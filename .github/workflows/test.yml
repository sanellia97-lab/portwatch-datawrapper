name: Test Datawrapper
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install requests
      - name: Test connessione
        run: python test_connection.py
        env:
          DW_TOKEN: ${{ secrets.DW_TOKEN }}
